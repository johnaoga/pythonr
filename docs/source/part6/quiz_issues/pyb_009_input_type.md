### Quiz ID

quiz1_python_basics

### Question Type

single (single choice)

### Question

Quel type de données retourne la fonction `input()` en Python ?

### Options (for single/multiple choice only)

int
float
str
Le type dépend de ce que l'utilisateur entre

### Answer

2

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`input()` retourne toujours une chaîne de caractères (`str`), même si l'utilisateur entre un nombre. Il faut utiliser `int()` ou `float()` pour convertir.
