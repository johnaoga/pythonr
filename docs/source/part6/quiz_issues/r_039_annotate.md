### Quiz ID

quiz5_r_visualization

### Question Type

multiple (multiple choices)

### Question

Quels types d'annotations peut-on ajouter avec la fonction `annotate()` de ggplot2 ?

### Options (for single/multiple choice only)

text
segment (flèche)
rect (rectangle)
circle (cercle parfait)

### Answer

0,1,2

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`annotate()` supporte "text", "segment" (avec arrow), "rect", "point", etc. Pour un cercle, on utilise `geom_point()` avec une grande taille.
