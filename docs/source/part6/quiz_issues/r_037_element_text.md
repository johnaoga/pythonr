### Quiz ID

quiz5_r_visualization

### Question Type

single (single choice)

### Question

Dans ggplot2, quelle fonction permet de personnaliser le texte dans `theme()` ?

### Options (for single/multiple choice only)

text_style()
font()
element_text()
style_text()

### Answer

2

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`element_text(size = 12, face = "bold", angle = 45)` personnalise le texte. Par exemple: `theme(axis.text.x = element_text(angle = 45))`.
