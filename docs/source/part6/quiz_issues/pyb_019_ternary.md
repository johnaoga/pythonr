### Quiz ID

quiz1_python_basics

### Question Type

single (single choice)

### Question

Quelle est la syntaxe correcte pour l'opérateur ternaire (condition sur une ligne) en Python ?

### Options (for single/multiple choice only)

condition ? valeur1 : valeur2
valeur1 if condition else valeur2
if condition then valeur1 else valeur2
valeur1 when condition otherwise valeur2

### Answer

1

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

Python utilise `valeur1 if condition else valeur2`. Exemple: `statut = "majeur" if age >= 18 else "mineur"`.
