### Quiz ID

quiz2_python_visualization

### Question Type

single (single choice)

### Question

Quelle méthode pandas est souvent utilisée pour préparer les données avant de créer une heatmap ?

### Options (for single/multiple choice only)

df.reshape()
df.pivot_table()
df.transform()
df.heatmap_prep()

### Answer

1

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`df.pivot_table(values='valeur', index='ligne', columns='colonne')` restructure les données en format matriciel, idéal pour les heatmaps.
