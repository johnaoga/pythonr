### Quiz ID

quiz2_python_visualization

### Question Type

single (single choice)

### Question

Quelle fonction Streamlit affiche un graphique Plotly interactif ?

### Options (for single/multiple choice only)

st.plot(fig)
st.plotly(fig)
st.plotly_chart(fig)
st.show_plotly(fig)

### Answer

2

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`st.plotly_chart(fig)` affiche un graphique Plotly. Pour Matplotlib, on utilise `st.pyplot(fig)`.
