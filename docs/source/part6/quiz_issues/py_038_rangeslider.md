### Quiz ID

quiz2_python_visualization

### Question Type

single (single choice)

### Question

Quelle méthode Plotly ajoute un sélecteur de plage interactif sur l'axe X ?

### Options (for single/multiple choice only)

fig.add_rangeslider()
fig.update_xaxes(rangeslider_visible=True)
fig.enable_slider()
fig.set_range_selector()

### Answer

1

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`fig.update_xaxes(rangeslider_visible=True)` ajoute un sélecteur de plage permettant de zoomer sur une période.
