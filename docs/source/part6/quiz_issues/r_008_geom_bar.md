### Quiz ID

quiz5_r_visualization

### Question Type

single (single choice)

### Question

Dans `geom_bar(stat = "identity")`, que signifie `stat = "identity"` ?

### Options (for single/multiple choice only)

Compter automatiquement les occurrences
Utiliser les valeurs y telles quelles
Calculer les pourcentages
Normaliser les données

### Answer

1

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`stat = "identity"` utilise directement les valeurs de y. Par défaut, `geom_bar()` compte les occurrences (`stat = "count"`).
