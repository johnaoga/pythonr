### Quiz ID

quiz2_python_visualization

### Question Type

multiple (multiple choices)

### Question

Quels sont des widgets d'entrée valides en Streamlit ?

### Options (for single/multiple choice only)

st.slider()
st.input()
st.selectbox()
st.file_uploader()
st.dropdown()

### Answer

0,2,3

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

Streamlit offre `st.slider()`, `st.selectbox()`, et `st.file_uploader()`. Il n'existe pas de `st.input()` (c'est `st.text_input()`) ni de `st.dropdown()` (c'est `st.selectbox()`).
