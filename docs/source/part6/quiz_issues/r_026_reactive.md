### Quiz ID

quiz5_r_visualization

### Question Type

single (single choice)

### Question

Quelle fonction Shiny crée une expression réactive qui se recalcule quand ses dépendances changent ?

### Options (for single/multiple choice only)

observe()
reactive()
compute()
auto()

### Answer

1

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`reactive({...})` crée une expression qui se recalcule automatiquement. On accède à sa valeur avec `data()` (parenthèses).
