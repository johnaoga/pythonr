### Quiz ID

quiz2_python_visualization

### Question Type

single (single choice)

### Question

Quelle fonction Streamlit affiche un DataFrame de manière interactive (triable, filtrable) ?

### Options (for single/multiple choice only)

st.table(df)
st.dataframe(df)
st.show(df)
st.display(df)

### Answer

1

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

`st.dataframe(df)` affiche un DataFrame interactif. `st.table(df)` affiche un tableau statique sans interactivité.
