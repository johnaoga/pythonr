### Quiz ID

quiz2_python_visualization

### Question Type

single (single choice)

### Question

Quel paramètre de `sns.heatmap()` définit la palette de couleurs ?

### Options (for single/multiple choice only)

color
palette
cmap
colorscheme

### Answer

2

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

Le paramètre `cmap` (colormap) définit la palette de couleurs, par exemple `cmap='coolwarm'` pour un dégradé bleu-rouge.
