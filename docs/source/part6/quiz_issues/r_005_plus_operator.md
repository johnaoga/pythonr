### Quiz ID

quiz5_r_visualization

### Question Type

single (single choice)

### Question

Quel opérateur utilise-t-on pour ajouter des couches à un graphique ggplot2 ?

### Options (for single/multiple choice only)

%>%
|>
+
&

### Answer

2

### Tolerance (number type only)

_No response_

### Case Sensitive (text type only)

_No response_

### Explanation (optional)

L'opérateur `+` est utilisé pour empiler les couches dans ggplot2: `ggplot(data, aes(x, y)) + geom_point() + theme_minimal()`.
